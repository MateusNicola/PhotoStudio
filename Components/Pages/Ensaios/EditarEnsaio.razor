@page "/ensaios/editarEnsaio/{EnsaioId:int}"
@rendermode InteractiveServer
@using PhotoStudio.app.Data
@using PhotoStudio.app.Models
@using PhotoStudio.app.Services
@using PhotoStudio.app.Services.Clientes
@using PhotoStudio.app.Services.Ensaios
@inject EnsaioService EnsaioService
@inject ClienteService ClienteService
@inject NavigationManager Navigation
@inject ToastService ToastService

<PageTitle>Editar Ensaio - Estúdio de Fotografia</PageTitle>

<div class="page-container">
    <div class="page-header">
        <div class="header-content">
            <button class="btn-back" @onclick="Voltar" title="Voltar">
                <span>←</span>
            </button>
            <div>
                <h1>Editar Ensaio</h1>
                <p class="subtitle">Atualize os dados do ensaio fotográfico</p>
            </div>
        </div>
    </div>

    @if (carregando)
    {
        <div class="loading-container">
            <div class="loading-spinner"></div>
            <p>Carregando dados do ensaio...</p>
        </div>
    }
    else if (ensaio == null)
    {
        <div class="error-container">
            <div class="error-icon">⚠️</div>
            <h3>Ensaio não encontrado</h3>
            <p>O ensaio que você está tentando editar não existe.</p>
            <button class="btn btn-primary" @onclick="Voltar">
                Voltar para Ensaios
            </button>
        </div>
    }
    else
    {
        <div class="form-container">
            <EditForm Model="@ensaio" OnValidSubmit="SalvarEnsaio">
                <DataAnnotationsValidator />

                <div class="form-grid">
                    <!-- Título -->
                    <div class="form-group full-width">
                        <label for="titulo">Título do Ensaio *</label>
                        <InputText id="titulo" @bind-Value="ensaio.Titulo"
                                   class="form-control" placeholder="Ex: Ensaio de Casamento - João e Maria" />
                        <ValidationMessage For="@(() => ensaio.Titulo)" />
                    </div>

                    <!-- Tipo de Ensaio -->
                    <div class="form-group">
                        <label for="tipo">Tipo de Ensaio *</label>
                        <InputSelect id="tipo" @bind-Value="ensaio.Tipo" class="form-control">
                            @foreach (var tipo in Enum.GetValues<TipoEnsaio>())
                            {
                                <option value="@tipo">@tipo</option>
                            }
                        </InputSelect>
                        <ValidationMessage For="@(() => ensaio.Tipo)" />
                    </div>

                    <!-- Cliente -->
                    <div class="form-group">
                        <div class="label-with-button">
                            <label for="cliente">Cliente *</label>
                        </div>
                        <select id="cliente" @bind="ensaio.ClienteId" class="form-control">
                            <option value="0">Selecione o cliente</option>
                            @foreach (var cliente in clientes)
                            {
                                <option value="@cliente.Id">@cliente.Nome</option>
                            }
                        </select>
                        @if (ensaio.ClienteId == 0)
                        {
                            <small class="validation-message">O campo Cliente é obrigatório.</small>
                        }
                    </div>

                    <!-- Data -->
                    <div class="form-group">
                        <label for="data">Data *</label>
                        <InputDate id="data" @bind-Value="dataEnsaio" class="form-control" />
                        <ValidationMessage For="@(() => dataEnsaio)" />
                    </div>

                    <!-- Hora -->
                    <div class="form-group">
                        <label for="hora">Horário *</label>
                        <InputText id="hora" type="time" @bind-Value="horaEnsaio" class="form-control" />
                        @if (string.IsNullOrEmpty(horaEnsaio))
                        {
                            <small class="validation-message">O campo Horário é obrigatório.</small>
                        }
                    </div>

                    <!-- Local -->
                    <div class="form-group full-width">
                        <label for="local">Local *</label>
                        <InputText id="local" @bind-Value="ensaio.Local"
                                   class="form-control" placeholder="Ex: Praça da Matriz, Porto Alegre" />
                        <ValidationMessage For="@(() => ensaio.Local)" />
                    </div>

                    <!-- Valor -->
                    <div class="form-group">
                        <label for="valor">Valor (R$)</label>
                        <InputNumber id="valor" @bind-Value="ensaio.Valor"
                                     class="form-control" placeholder="0,00" />
                        <ValidationMessage For="@(() => ensaio.Valor)" />
                    </div>

                    <!-- Situação -->
                    <div class="form-group">
                        <label for="situacao">Situação *</label>
                        <InputSelect id="situacao" @bind-Value="ensaio.Situacao" class="form-control">
                            @foreach (var situacao in Enum.GetValues<Situacao>())
                            {
                                <option value="@situacao">@situacao</option>
                            }
                        </InputSelect>
                        <ValidationMessage For="@(() => ensaio.Situacao)" />
                    </div>
                </div>

                <!-- Botões de Ação -->
                <div class="form-actions">
                    <button type="button" class="btn btn-secondary" @onclick="Voltar" disabled="@salvando">
                        Cancelar
                    </button>
                    <button type="submit" class="btn btn-primary" disabled="@salvando">
                        @if (salvando)
                        {
                            <span class="spinner"></span>
                            <span>Salvando...</span>
                        }
                        else
                        {
                            <span>💾 Salvar Alterações</span>
                        }
                    </button>
                </div>
            </EditForm>
        </div>
    }
</div>

<style>
    .page-container {
        max-width: 900px;
        margin: 0 auto;
        padding: 20px;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
    }

    .page-header {
        margin-bottom: 32px;
    }

    .header-content {
        display: flex;
        align-items: flex-start;
        gap: 16px;
    }

    .btn-back {
        background: #f7fafc;
        border: 2px solid #e2e8f0;
        border-radius: 10px;
        width: 48px;
        height: 48px;
        font-size: 1.5rem;
        cursor: pointer;
        transition: all 0.2s;
        color: #4a5568;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
    }

        .btn-back:hover {
            background: #edf2f7;
            border-color: #cbd5e0;
            transform: translateX(-4px);
        }

    .page-header h1 {
        font-size: 2.5rem;
        color: #2d3748;
        margin: 0 0 8px 0;
    }

    .subtitle {
        color: #718096;
        font-size: 1.1rem;
        margin: 0;
    }

    .form-container {
        background: white;
        border-radius: 12px;
        padding: 32px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    .form-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 24px;
        margin-bottom: 32px;
    }

    .form-group {
        display: flex;
        flex-direction: column;
    }

        .form-group.full-width {
            grid-column: 1 / -1;
        }

    .label-with-button {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 8px;
    }

    label {
        font-weight: 600;
        color: #2d3748;
        margin: 0;
        font-size: 0.95rem;
    }

    .btn-link {
        background: none;
        border: none;
        color: #667eea;
        font-size: 0.85rem;
        font-weight: 600;
        cursor: pointer;
        padding: 4px 8px;
        border-radius: 4px;
        transition: all 0.2s;
    }

        .btn-link:hover {
            background: #eef2ff;
            color: #5a67d8;
        }

    .form-control {
        padding: 12px 16px;
        border: 2px solid #e2e8f0;
        border-radius: 8px;
        font-size: 1rem;
        transition: all 0.2s;
        background: white;
        color: #2d3748;
    }

        .form-control:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        .form-control::placeholder {
            color: #a0aec0;
        }

    select.form-control {
        cursor: pointer;
    }

    .validation-message {
        color: #e53e3e;
        font-size: 0.85rem;
        margin-top: 6px;
    }

    .form-actions {
        display: flex;
        gap: 12px;
        justify-content: flex-end;
    }

    .btn {
        padding: 12px 32px;
        border: none;
        border-radius: 8px;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.2s;
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .btn-primary {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
    }

        .btn-primary:hover:not(:disabled) {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
        }

        .btn-primary:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }

    .btn-secondary {
        background: #f7fafc;
        color: #4a5568;
        border: 2px solid #e2e8f0;
    }

        .btn-secondary:hover:not(:disabled) {
            background: #edf2f7;
            border-color: #cbd5e0;
        }

        .btn-secondary:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }

    .spinner {
        width: 16px;
        height: 16px;
        border: 2px solid rgba(255,255,255,0.3);
        border-top-color: white;
        border-radius: 50%;
        animation: spin 0.6s linear infinite;
    }

    @@keyframes spin {
        to {
            transform: rotate(360deg);
        }
    }

    /* Loading State */
    .loading-container {
        background: white;
        border-radius: 12px;
        padding: 60px 32px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        text-align: center;
    }

    .loading-spinner {
        width: 48px;
        height: 48px;
        border: 4px solid #e2e8f0;
        border-top-color: #667eea;
        border-radius: 50%;
        animation: spin 0.8s linear infinite;
        margin: 0 auto 20px;
    }

    .loading-container p {
        color: #718096;
        font-size: 1.1rem;
        margin: 0;
    }

    /* Error State */
    .error-container {
        background: white;
        border-radius: 12px;
        padding: 60px 32px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        text-align: center;
    }

    .error-icon {
        font-size: 4rem;
        margin-bottom: 16px;
    }

    .error-container h3 {
        color: #2d3748;
        font-size: 1.8rem;
        margin: 0 0 12px 0;
    }

    .error-container p {
        color: #718096;
        font-size: 1.1rem;
        margin: 0 0 24px 0;
    }

    /* Responsivo */
    @@media (max-width: 768px) {
        .form-grid {
            grid-template-columns: 1fr;
        }

        .form-group.full-width {
            grid-column: 1;
        }

        .form-actions {
            flex-direction: column-reverse;
        }

        .btn {
            width: 100%;
            justify-content: center;
        }

        .header-content {
            flex-direction: column;
            gap: 12px;
        }

        .btn-back {
            align-self: flex-start;
        }

        .page-header h1 {
            font-size: 2rem;
        }
    }
</style>

@code {
    [Parameter] public int EnsaioId { get; set; }

    private EnsaioModel? ensaio;
    private List<ClienteModel> clientes = new();
    private bool carregando = true;
    private bool salvando = false;
    private DateTime dataEnsaio = DateTime.Today;
    private string horaEnsaio = "10:00";

    protected override async Task OnInitializedAsync()
    {
        try
        {
            // Carrega dados em paralelo para maior rapidez
            var tarefas = new Task[] {
                CarregarClientes(),
                CarregarEnsaio()
            };
            await Task.WhenAll(tarefas);
        }
        finally
        {
            carregando = false;
        }
    }

    private async Task CarregarClientes()
    {
        clientes = await ClienteService.ListarTodosAsync();
    }

    private async Task CarregarEnsaio()
    {
        ensaio = await EnsaioService.ObterPorIdAsync(EnsaioId);

        if (ensaio != null)
        {
            dataEnsaio = ensaio.Data.Date;
            horaEnsaio = ensaio.Data.ToString("HH:mm");
        }
    }

    private async Task SalvarEnsaio()
    {
        if (ensaio == null || ensaio.ClienteId == 0) return;

        salvando = true;
        try
        {
            // Sincroniza Data e Hora
            if (TimeSpan.TryParse(horaEnsaio, out var hora))
            {
                ensaio.Data = dataEnsaio.Date.Add(hora);
            }

            await EnsaioService.AtualizarAsync(ensaio);
            ToastService.Success("Ensaio atualizado com sucesso!");

            await Task.Delay(500);
            Navigation.NavigateTo("/ensaios");
        }
        catch (Exception ex)
        {
            ToastService.Error($"Erro ao atualizar: {ex.Message}");
        }
        finally
        {
            salvando = false;
        }
    }

    private void Voltar() => Navigation.NavigateTo("/ensaios");
}